# MySQL database (read from Docker environment variables)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/learninghub}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Beshu@2107}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server config
server.port=${SERVER_PORT:8080}
server.address=${SERVER_ADDRESS:0.0.0.0}
server.servlet.context-path=${SERVER_CONTEXT_PATH:/back1}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Mail config (optional to leave as-is or make environment variables)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=learninghubfsad@gmail.com
spring.mail.password=hxbohuzdaggrxanc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
